# Zoho OAuth for server-to-server (use refresh token flow)
ZOHO_CLIENT_ID=your_client_id
ZOHO_CLIENT_SECRET=your_client_secret
ZOHO_REFRESH_TOKEN=your_refresh_token
ZOHO_ACCOUNTS_DOMAIN=https://accounts.zoho.com  # adjust if using .eu or .in etc.

# Creator / People identifiers
ZOHO_OWNER_NAME=your_zoho_owner_name  # e.g. companyname or email prefix in creator URLs
CREATOR_APP_LINK=your_app_link_name
# Note: Creator uses API v2: https://creator.zoho.com/api/v2/{owner}/{app}/...
# For People we will use people endpoints

# Optional: direct access token caching (not recommended for prod)
ACCESS_TOKEN_CACHE_TTL=300   # seconds - refresh safety

# For debugging / environment
NODE_ENV=development
